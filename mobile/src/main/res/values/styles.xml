<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">

    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!--Setting Theme for pre-lollipop -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorControlHighlight">@android:color/white</item>

    </style>

    <!--this properties set for Toolbar-->
    <!--setting overlay theme and secondaryColor of Toolbar-->
    <style name="myThemeForToolbar" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorSecondary">#fff</item>

    </style>
    <style name="AppThemeCustom..PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">2dp</item>
        <item name="android:color">@color/com_facebook_blue</item>

    </style>

</resources>
